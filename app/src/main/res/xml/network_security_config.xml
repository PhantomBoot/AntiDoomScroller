<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The app makes no HTTP requests of its own. Plaintext is denied outright. -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
